import { BriefcaseBusiness, CheckCircle2 } from "lucide-react";
import { experiences } from "@/data/experience";
import SectionHeading from "./SectionHeading";
export default function Experience() { return <section id="experience" className="section"><div className="container"><SectionHeading title="Work Experience" description="การทำงานที่สอนให้รับผิดชอบ สื่อสาร และส่งมอบสิ่งที่มีคุณค่า ไม่ใช่เพียงเขียนโค้ดให้เสร็จ" /><div className="experience-list">{experiences.map((item) => <article key={item.role}><div className="experience-icon"><BriefcaseBusiness /></div><div className="experience-content"><div className="experience-head"><div><span>{item.type}</span><h3>{item.role}</h3><p>{item.organization}</p></div><time>{item.period}</time></div><ul>{item.responsibilities.map((text) => <li key={text}><CheckCircle2 /> {text}</li>)}</ul><p className="achievement"><strong>Highlight:</strong> {item.achievements.join(" · ")}</p><div className="tags">{item.technologies.map((tech) => <span key={tech}>{tech}</span>)}</div></div></article>)}</div><div className="opportunity"><div><span>กำลังมองหาโอกาสใหม่</span><h3>พร้อมนำความรู้ด้าน Software ไปใช้กับงานจริง</h3></div><a className="button" href="#contact">Contact Me</a></div></div></section>; }
